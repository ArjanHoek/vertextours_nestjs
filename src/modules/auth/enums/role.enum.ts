export enum Role {
  User = 'USER', // Regular user who can read and book refuges and tours
  Admin = 'ADMIN', // User with create/update/delete permissons on all data
  Manager = 'MANAGER', // User with permissions to create tours
}
